node_modules
.next
.env.local
*.tsbuildinfo
next-env.d.ts
.DS_Store
.superpowers/

# Reference implementations for workstream D (offline games) — not part of the app build
Offline Games/

# Bug automation — never commit the service-account key
.gsheets-key.json
